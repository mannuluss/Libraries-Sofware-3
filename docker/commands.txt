# creacion de la red
docker network create catalog-network

# despliegue del contenedor backend de catalogo
docker run --network=catalog-network --name backend-catalog-cont -d -p 8081:8081 backend-catalog-image

# despliegue del contenedor frontend de catalogo
docker run --network=catalog-network --name fronted-catalog-cont -d -p 81:80 frontend-catalog-image